In order to run this code you have to have Node installed

First install all the packages using "npm install"

If you want to scrape the data using Puppeteer then:
=> run the server using this command "node server.js"

If you want to scrape the data using Cheerio and request-promise then:
=> run the server using this command "node usingCheerio.js"